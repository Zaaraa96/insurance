import accept from './components/accept.vue';
import form from './components/form.vue';
export const routes=[
            {path: '/', component: accept},
            {path: '/form', component: form},
          ];
